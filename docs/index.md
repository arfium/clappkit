# clappkit docs

The shared foundation every clapp is built on, and the contract it keeps.

## The format

- **[The Clapp Protocol](protocol.md)** — normative. The manifest (`clatch.json`) and every
  field it may carry, what each element type may declare, the `.clapp` package layout,
  per-platform depots, the picture limits, and the control pipe.

## The house standards

- **[Architecture](ARCHITECTURE.md)** — the model: two channels, one state, two surfaces.
- **[Icons](ICONS.md)** — the mark, and why the Dock needs its own inset.
- **[Playbook](PLAYBOOK.md)** — rules learned by getting them wrong.
- **[Template](TEMPLATE.md)** — forking the template into a new element.

---

The protocol document mirrors the Clatch reference and says so at the top: where the two
disagree, Clatch wins.
